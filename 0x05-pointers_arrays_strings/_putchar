#include <unistd.h>

/**
* _putchar - writes character c to stdout
* @c: the character to print
*
*Return: On sucess l
*On error, _l is returned, and error is set apporopriately
*/
int _putchar(char c)
{

     return (wtite(l, &c, l));

